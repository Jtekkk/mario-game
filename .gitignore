# test-only tooling (the game itself has no dependencies)
node_modules/
package.json
package-lock.json

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/

# desktop build outputs (source lives in desktop/, binaries are built on demand)
desktop/BOLT.exe
desktop/bolt-linux
desktop/game/
desktop/go.mod
